# 环境变量配置模板

## 📝 VPS 部署时需要配置的环境变量

将以下内容复制到 VPS 的 `/opt/focusai/backend/.env` 文件中：

```bash
# ============================================
# SiliconFlow (硅基流动) DeepSeek API
# ============================================
SILICONFLOW_API_KEY=sk-你的密钥
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1
DEEPSEEK_MODEL=deepseek-ai/DeepSeek-V3

# ============================================
# Supabase Database
# ============================================
SUPABASE_URL=https://eyqnsyxhkbhlloqmvncc.supabase.co
SUPABASE_ANON_KEY=你的anon-key
SUPABASE_SERVICE_KEY=你的service-key

# ============================================
# Tavily Search API
# ============================================
TAVILY_API_KEYS=tvly-你的密钥

# ============================================
# Application Settings
# ============================================
CRAWL_INTERVAL_HOURS=6
API_PORT=8000

# ⚠️ 生产环境必须设为 false
DEBUG=false

# ============================================
# Admin Security (管理后台安全)
# ============================================
# ⚠️ 请设置一个强密码
ADMIN_PASSWORD=你的强密码

# ============================================
# Production Settings (生产环境配置)
# ============================================
# ⚠️ 替换为你的 VPS IP 地址
ALLOWED_ORIGINS=http://你的VPS_IP
```

## 🔑 如何获取 API Keys

### 1. SiliconFlow API Key
- 访问：https://cloud.siliconflow.cn/
- 注册并创建 API Key

### 2. Supabase Keys
- 访问：https://supabase.com/dashboard
- 选择你的项目
- Settings → API → 复制 URL 和 Keys

### 3. Tavily API Key
- 访问：https://tavily.com/
- 注册账号（免费 1000 次/月）
- 获取 API Key

## ⚠️ 安全提醒

1. **不要**将 `.env` 文件提交到 Git
2. **不要**在公开场合分享这些密钥
3. **定期**更换管理员密码
4. **使用**强密码（至少 12 位，包含大小写字母、数字、特殊字符）

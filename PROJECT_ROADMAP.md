# FocusAI 项目规划书
> 产品代号：FocusAI (职场 AI 之窗)  
> 核心理念：News-to-Action (从资讯到行动)

---

## 一、产品定位

**一句话描述**：为职场人打造的 AI 资讯私人助理，不仅帮你筛选信息，更帮你把新闻变成可直接使用的生产力资产。

**核心价值**：
- 🎯 **精准过滤**：只看与你职业相关的 AI 动态
- 💡 **深度解读**：每条新闻附带"对你意味着什么"的职业建议
- 📋 **一键获取**：Prompt、指令、工具链接一键复制
- 🔄 **越用越懂你**：通过反馈机制训练专属推荐模型

---

## 二、项目进度

### ✅ 已完成

| 模块 | 状态 | 说明 |
|------|------|------|
| 产品定位 | ✅ | MVP 功能范围确定 |
| 前端 UI | ✅ | React + Tailwind，位于 `focusai/` |
| 后端 API | ✅ | FastAPI，位于 `backend/` |
| 新闻爬虫 | ✅ | 支持 RSS、网页、API |
| AI 处理 | ✅ | DeepSeek 生成洞察卡片 |
| 数据库设计 | ✅ | Supabase SQL 脚本 |

### ⏳ 待完成

| 任务 | 优先级 |
|------|--------|
| 配置 Supabase 数据库 | P0 |
| 配置硅基流动 API Key | P0 |
| 前后端联调 | P0 |
| 部署上线 | P1 |

---

## 三、项目结构

```
私人定制AI新闻网站/
├── focusai/                 # 前端 (React + Vite)
│   ├── App.tsx
│   ├── components/
│   │   ├── Header.tsx
│   │   ├── InsightCard.tsx
│   │   ├── LandingPage.tsx
│   │   ├── Settings.tsx
│   │   └── ...
│   ├── types.ts
│   └── constants.ts
│
├── backend/                 # 后端 (FastAPI)
│   ├── main.py              # API 主程序
│   ├── config.py            # 配置
│   ├── models.py            # 数据模型
│   ├── database.py          # Supabase 操作
│   ├── routers/
│   │   ├── insights.py      # 卡片 API
│   │   ├── users.py         # 用户 API
│   │   └── interactions.py  # 反馈 API
│   ├── services/
│   │   ├── ai_processor.py  # DeepSeek 处理
│   │   └── news_crawler.py  # 新闻爬虫
│   ├── run_crawler.py       # 爬虫脚本
│   ├── init_database.sql    # 数据库初始化
│   ├── requirements.txt
│   └── .env.example
│
├── PROJECT_ROADMAP.md
└── LANDING_PAGE_COPY.md     # 主页文案
```

---

## 四、后端 API 列表

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/insights` | 获取洞察卡片列表 |
| GET | `/api/insights/mock` | Mock 数据 (开发用) |
| GET | `/api/insights/{id}` | 单个卡片详情 |
| POST | `/api/interactions` | 记录用户反馈 |
| GET | `/api/bookmarks` | 获取收藏列表 |
| GET | `/api/user/profile` | 获取用户信息 |
| PUT | `/api/user/profile` | 更新用户信息 |
| GET | `/api/user/professions` | 获取职业列表 |

---

## 五、快速开始

### 1. 后端启动

```bash
cd backend

# 安装依赖
pip install -r requirements.txt

# 复制配置文件
cp .env.example .env
# 编辑 .env，填入 API Key

# 启动服务
python main.py
```

### 2. 前端启动

```bash
cd focusai

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

### 3. 数据库配置

1. 注册 [Supabase](https://supabase.com/)
2. 创建新项目
3. 在 SQL Editor 中运行 `backend/init_database.sql`
4. 复制 URL 和 Key 到 `.env`

### 4. 运行爬虫

```bash
cd backend
python run_crawler.py
```

---

## 六、技术栈

| 层级 | 技术 |
|------|------|
| 前端 | React + TypeScript + Tailwind CSS + Vite |
| 后端 | Python FastAPI |
| AI | DeepSeek (via 硅基流动 SiliconFlow) |
| 数据库 | PostgreSQL (Supabase) |
| 爬虫 | httpx + BeautifulSoup + feedparser |

---

## 七、阶段规划

### 第一阶段：MVP ✅ 进行中
- [x] 确定产品定位
- [x] 前端页面开发
- [x] 后端 API 开发
- [x] 爬虫 + AI 处理
- [ ] 数据库配置
- [ ] 前后端联调
- [ ] 部署上线

### 第二阶段：个性化
- [ ] 用户登录系统
- [ ] 推荐算法
- [ ] 打分功能

### 第三阶段：资产化
- [ ] Prompt 资源库
- [ ] 技能树可视化

### 第四阶段：商业化
- [ ] 会员订阅
- [ ] 高级功能

---

*最后更新：2024年12月3日*
